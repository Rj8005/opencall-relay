<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#1A1A1A"
    android:fillViewport="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="12dp"
        android:paddingBottom="32dp">

        <!-- Drag handle -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="20dp"
            android:background="#444444"/>

        <!-- Header: phone number -->
        <TextView
            android:id="@+id/invite_number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="+1 555 000 0000 is not on OpenCall yet"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp"/>

        <!-- Header: country hint -->
        <TextView
            android:id="@+id/invite_country"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Best options for US:"
            android:textColor="#888888"
            android:textSize="13sp"
            android:layout_marginBottom="20dp"/>

        <!-- Invite URL (read-only) -->
        <EditText
            android:id="@+id/invite_link"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="https://opencall.net/join/…"
            android:textColor="#BFFF00"
            android:textSize="13sp"
            android:background="#111111"
            android:padding="14dp"
            android:inputType="none"
            android:focusable="false"
            android:focusableInTouchMode="false"
            android:cursorVisible="false"
            android:singleLine="true"
            android:ellipsize="end"
            android:layout_marginBottom="10dp"/>

        <!-- Copy link button -->
        <Button
            android:id="@+id/copy_btn"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Copy Link"
            android:textColor="#BFFF00"
            android:textSize="14sp"
            android:textAllCaps="false"
            android:backgroundTint="@android:color/transparent"
            android:stateListAnimator="@null"
            android:layout_marginBottom="24dp"/>

        <!-- Divider label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="── send directly ──"
            android:textColor="#3A3A3A"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- Channel grid — populated dynamically by showInvitePanel -->
        <com.google.android.flexbox.FlexboxLayout
            android:id="@+id/channel_grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:flexWrap="wrap"
            app:justifyContent="flex_start"
            app:alignItems="stretch"
            android:layout_marginBottom="24dp"/>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#242424"
            android:layout_marginBottom="20dp"/>

        <!-- TextBelt auto-SMS button -->
        <Button
            android:id="@+id/textbelt_btn"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Send SMS automatically"
            android:textColor="#0A0A0A"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textAllCaps="false"
            android:backgroundTint="#BFFF00"
            android:stateListAnimator="@null"
            android:layout_marginBottom="6dp"/>

        <!-- TextBelt status -->
        <TextView
            android:id="@+id/textbelt_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="#888888"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginBottom="10dp"/>

        <!-- Share via any app -->
        <Button
            android:id="@+id/share_btn"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Share via any app"
            android:textColor="#CCCCCC"
            android:textSize="14sp"
            android:textAllCaps="false"
            android:backgroundTint="@android:color/transparent"
            android:stateListAnimator="@null"/>

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
